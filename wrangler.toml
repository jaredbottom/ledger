name = "ledger"
pages_build_output_dir = "dist"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Set TURSO_DATABASE_URL and TURSO_AUTH_TOKEN via:
#   - .dev.vars for local development
#   - Cloudflare Pages dashboard for production (Settings → Environment variables)
